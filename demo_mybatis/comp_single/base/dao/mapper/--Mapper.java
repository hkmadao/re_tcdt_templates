package {{ rootInfo.basePath }}.{{ rootInfo.packageName}}.dao.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import {{ rootInfo.basePath }}.{{ rootInfo.packageName}}.dao.entity.{{ rootInfo.className }};

public interface {{ rootInfo.className }}Mapper extends BaseMapper<{{ rootInfo.className }}> {

}
